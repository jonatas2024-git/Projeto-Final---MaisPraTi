spring.application.name=portaldevagas
spring.datasource.url=jdbc:postgresql://${DB_HOST_POSTGRES}/dbportalvagas
spring.datasource.username=${DB_USER_POSTGRES}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#precisamos trocar essa secret key para finalizar o projeto
api.security.token.secret=my-secret-key-from-project